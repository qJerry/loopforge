<!-- Generated by LoopForge onboarding v2; seed -->
# {{PROJECT_NAME}} 项目组构建入口

## 聚合脚本

父仓提供 format、format-check、lint、test、build 五个统一入口。初始模板会明确失败，必须由项目维护者根据已登记子仓配置调用顺序。

聚合脚本应：

- 使用明确的子仓相对路径或项目配置，不扫描相邻目录。
- 逐仓调用子仓自身 `scripts/*.sh`，不复制技术栈命令。
- 在首个失败处返回非零，并指出失败子仓和命令。
- 不安装依赖、不启动服务、不发布、不合入分支。
- 若确需并行，保留每个子仓独立日志和退出码。

## 变更同步

新增、移除或重命名子仓时，同步 `PROJECT_GROUP.md`、项目配置和五个聚合脚本。子仓新增项目专用命令不自动提升为父仓标准门禁。

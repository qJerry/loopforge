<!-- Generated by LoopForge onboarding v2; seed -->
# {{PROJECT_NAME}} 项目组架构约束

## 边界

父仓只维护跨仓拓扑、长期系统边界和聚合入口。每个子仓独立维护源码、依赖、构建、测试和技术栈规范。

- 跨仓调用方向、协议所有者和发布兼容写入 `docs/system/design.md` 或实际系统文档。
- 子仓内部重构不应要求父仓复制包级细节。
- 共享协议必须指定单一所有者、版本策略和消费者验证。
- 父仓不通过脚本静默修改子仓源码、分支或任务状态。

## 任务拓扑

没有 `targets[]` 的任务在父仓自身执行；有显式 Target 时只路由到 `PROJECT_GROUP.md` 登记的子仓。Target 的 `scope` 定义职责，`after` 只表达必要依赖。

## 禁止事项

- 把相邻 Git 仓库自动视为项目成员。
- 在父仓重复维护子仓状态枚举、schema 或完整工程规范。
- 用父仓聚合脚本掩盖子仓门禁失败。
- 让跨仓协调任务在子仓创建镜像 backlog item。

<!-- Generated by LoopForge onboarding v2; seed -->
# {{PROJECT_NAME}} 项目组测试约束

## 验证原则

- 单仓改动先通过该仓自身门禁。
- 跨仓任务按 Target 拓扑逐仓验证，并保留每个 Target 的真实命令和结果。
- 协议提供方变化必须验证所有受影响消费者；仅内部实现变化不强制运行无关仓库。
- 父仓聚合通过不等于端到端行为通过，E2E 由系统文档指定的可重复场景负责。

## 失败定位

聚合输出必须包含子仓 key、命令和退出码。一个 Target 阻塞时，只阻塞依赖它的后继；无依赖 Target 可以继续。

## 安全边界

项目组验收不连接生产、不使用真实凭证、不触发发布或远程合入。无法获得外部环境时明确记录未验证链路。

<!-- Generated by LoopForge onboarding v2; seed -->
# {{PROJECT_NAME}} Flutter 数据契约

## 数据边界

网络 DTO、本地持久化模型和 UI model 分别建模，在 repository/service 边界显式转换。具体业务字段仍以模块 specs 和服务端协议为准。

- JSON 缺失、null、未知 enum 和版本升级必须有明确策略。
- `fromJson/toJson` 生成代码不得手改；修改源模型后运行项目登记的 codegen。
- 缓存必须记录有效期、用户/租户边界和失效条件，不把陈旧缓存伪装成成功响应。
- 离线写入、冲突和重试需要幂等键或明确用户决策。

## 本地数据

- SharedPreferences 只保存非敏感轻量偏好。
- token、密钥和个人敏感数据使用项目批准的安全存储，并考虑设备备份与注销清理。
- schema 升级必须测试旧版本迁移和失败恢复；禁止删除用户数据作为默认修复。

## 展示与协议

- 金额、比例、时间、时区、分页和枚举规则写入模块 specs。
- 认证失败、权限不足、网络错误、服务错误和无数据分别映射。
- 日志、崩溃报告和测试 fixture 不包含真实凭证或隐私数据。

新增字段前确认来源、空值、版本兼容、缓存、敏感性和所有消费者。

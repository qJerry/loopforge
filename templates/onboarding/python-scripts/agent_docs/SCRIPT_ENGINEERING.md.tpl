<!-- Generated by LoopForge onboarding v2; seed -->
# {{PROJECT_NAME}} Python 脚本工程规范

## CLI 合同

- `--help` 不访问网络、不要求业务配置，退出码为 0。
- 参数错误写 stderr 并返回 2；运行失败返回非零；成功只在结果真实完成后返回 0。
- stdout 用于可消费结果，stderr 用于诊断；JSON 模式必须输出单个有效对象。
- 会修改文件或外部状态的入口优先提供 `--dry-run` 和明确目标摘要。

## 配置与安全

- 优先顺序必须明确：CLI 参数、环境变量、配置文件、默认值。
- 路径在写入前 resolve 并验证边界；不要对空变量、根目录或宽泛 glob 执行删除/覆盖。
- secrets 不进入命令行回显、日志、fixture 和错误堆栈。
- 网络请求设置连接/读取超时，区分 4xx、5xx 和传输错误。

## 幂等与恢复

- 重复执行不应重复追加、重复发送或破坏已完成结果。
- 覆盖、删除和迁移操作先预检精确目标，优先原子写入和可恢复替换。
- 批处理记录成功、失败和跳过项；单项失败策略必须显式。
- signal/异常路径清理临时文件、锁和子进程。

## 可观测性

日志包含操作、目标和结果，不记录敏感 payload。机器模式使用稳定字段；人类模式保持简洁可行动。
